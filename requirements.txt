# Core annotation app (Python 3.10)
flask==3.1.3
numpy<2            # TensorFlow 2.14 is built against NumPy 1.x; NumPy 2.x breaks its C extension
mido==1.3.3
soundfile==0.14.0
librosa==0.11.0

# AI fingering (optional; also needs fluidsynth + ffmpeg system packages)
midi2audio==0.1.1
midiutil==1.2.1
tensorflow>=2.14,<2.16
keras>=3.0        # ai_fingering.py runs standalone Keras 3 via a compatibility shim
